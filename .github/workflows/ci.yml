name: Commit

on:
  push:
  pull_request:
    branches:
      - master

env:
  CI: true
  CARGO_TERM_COLOR: always

permissions:
  contents: write

jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Install Protoc
        uses: arduino/setup-protoc@v3

      - name: Build
        run: cargo build --features all

      - name: Run tests
        run: cargo test --features all

  ECBDaily:
    runs-on: ubuntu-latest
    needs:
      - CI
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build ECB Daily
        uses: docker/build-push-action@4a13e500e55cf31b7a5d59a38ab2040ab0f42f56
        with:
          context: .
          push: false
          build-args: |
            BINARY=ecb-daily
            FEATURES=ecb


  ISSAstros:
    runs-on: ubuntu-latest
    needs:
      - CI
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build ISS Astros
        uses: docker/build-push-action@4a13e500e55cf31b7a5d59a38ab2040ab0f42f56
        with:
          context: .
          push: false
          build-args: |
            BINARY=iss-astros
            FEATURES=iss


  ISSPosition:
    runs-on: ubuntu-latest
    needs:
      - CI
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build ISS Position
        uses: docker/build-push-action@4a13e500e55cf31b7a5d59a38ab2040ab0f42f56
        with:
          context: .
          push: false
          build-args: |
            BINARY=iss-position
            FEATURES=iss


  Pinger:
    runs-on: ubuntu-latest
    needs:
      - CI
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build Pinger
        uses: docker/build-push-action@4a13e500e55cf31b7a5d59a38ab2040ab0f42f56
        with:
          context: .
          push: false
          build-args: |
            BINARY=pinger
            FEATURES=pinger


  RSS:
    runs-on: ubuntu-latest
    needs:
      - CI
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build RSS
        uses: docker/build-push-action@4a13e500e55cf31b7a5d59a38ab2040ab0f42f56
        with:
          context: .
          push: false
          build-args: |
            BINARY=rss
            FEATURES=rss

  Ticker:
    runs-on: ubuntu-latest
    needs:
      - CI
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build Ticker
        uses: docker/build-push-action@4a13e500e55cf31b7a5d59a38ab2040ab0f42f56
        with:
          context: .
          push: false
          build-args: |
            BINARY=ticker
            FEATURES=ticker


  Yahoo:
    runs-on: ubuntu-latest
    needs:
      - CI
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build Yahoo
        uses: docker/build-push-action@4a13e500e55cf31b7a5d59a38ab2040ab0f42f56
        with:
          context: .
          push: false
          build-args: |
            BINARY=yahoo
            FEATURES=yahoo

